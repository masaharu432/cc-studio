<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- API<29 の legacy Downloads 保存用。API29+ は MediaStore で権限不要。 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:localeConfig="@xml/locales_config"
        android:usesCleartextTraffic="false">

        <!--
          configChanges を網羅して構成変化で Activity を再生成させない。再生成は onCreate を
          走らせ全スクリーンの WebView を作り直す＝全スクリーンがリロードされ、各 code-server の
          ハートビート/実行中ターンも切れる。とくに DayNight テーマでは他アプリから戻る際の
          uiMode（夜間モード）や density/fontScale 変化が再生成を誘発していた。
          WebView 内容(code-server)は自前でレイアウト/テーマを管理するので、これらを手放しても問題ない。
        -->
        <!--
          windowSoftInputMode は adjustResize を明示する。未指定（adjustUnspecified）だと
          システムが resize/pan を自動判定し、外部ブラウザ往復後に pan 相当へ化けることがある。
          その状態ではキーボードが WebView に被さるだけで body(ICB) が縮まず、workbench が
          再レイアウトできずチャット入力欄がキーボードの裏に隠れる（実測: 正常時 bd:438 /
          往復後 bd:750 のまま・vv/in のみ 438）。
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|keyboard|navigation|uiMode|density|fontScale|locale|layoutDirection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- アプリ内の言語選択（AppCompatDelegate.setApplicationLocales）を自動永続化する。 -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>
</manifest>
