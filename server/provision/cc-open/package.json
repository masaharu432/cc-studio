{
  "name": "cc-open",
  "displayName": "CC Open",
  "description": "チャットのファイルリンクから開いた .md を editorAssociations のプレビュー(vscode.markdown.preview.editor)へ toggleEditorType でその場切替する補助拡張。HTML は専用拡張 aios-html-auto-preview が担当。",
  "publisher": "ccstudio",
  "version": "0.0.8",
  "engines": {
    "vscode": "^1.80.0"
  },
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "CC Open",
      "properties": {
        "cc-open.autoPreview.markdown": {
          "type": "boolean",
          "default": true,
          "description": "テキストで開かれた .md を自動でプレビュー(vscode.markdown.preview.editor)に切り替える"
        }
      }
    }
  }
}
